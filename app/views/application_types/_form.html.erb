<%= form_for(@application_type) do |f| %>
  <% if @application_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_type.errors.count, "error") %> prohibited this napplication_type from being saved:</h2>

      <ul>
      <% @application_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no_of_application %><br>
    <%= f.text_field :no_of_application %>
  </div>
  <div class="field">
    <%= f.label :path %><br>
    <%= f.text_field :path %>
  </div>
  <div class="actions">
    <%= f.submit  "Create" %>
  </div>
<% end %>
